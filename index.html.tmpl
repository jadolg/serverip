<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Exit IP Information</title>
    <style>
        :root {
            --bg-color: #f4f7f6;
            --card-bg: #ffffff;
            --text-color: #333;
            --secondary-text: #666;
            --accent-color: #2c3e50;
            --border-color: #eee;
        }

        @media (prefers-color-scheme: dark) {
            :root {
                --bg-color: #1a1a1a;
                --card-bg: #2d2d2d;
                --text-color: #e0e0e0;
                --secondary-text: #aaa;
                --accent-color: #3498db;
                --border-color: #444;
            }
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: var(--card-bg);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            max-width: 500px;
            width: 90%;
        }

        h1 {
            margin-top: 0;
            font-size: 1.5rem;
            color: var(--accent-color);
            border-bottom: 2px solid var(--border-color);
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: auto 1fr;
            gap: 12px 20px;
        }

        .label {
            font-weight: 600;
            color: var(--secondary-text);
        }

        .value {
            word-break: break-all;
        }

        .badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 0.8rem;
            font-weight: bold;
            background-color: var(--border-color);
        }

        .badge.tor {
            background-color: #e74c3c;
            color: white;
        }

        .footer {
            margin-top: 30px;
            font-size: 0.8rem;
            color: var(--secondary-text);
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Server IP Details</h1>
        <div class="info-grid">
            <div class="label">IP Address</div>
            <div class="value"><strong>{{ .YourFuckingIPAddress }}</strong></div>

            <div class="label">Location</div>
            <div class="value">{{ .YourFuckingLocation }}</div>

            <div class="label">City</div>
            <div class="value">{{ .YourFuckingCity }}</div>

            <div class="label">Country</div>
            <div class="value">{{ .YourFuckingCountry }} ({{ .YourFuckingCountryCode }})</div>

            <div class="label">ISP</div>
            <div class="value">{{ .YourFuckingISP }}</div>

            <div class="label">Hostname</div>
            <div class="value">{{ .YourFuckingHostname }}</div>

            <div class="label">Tor Exit</div>
            <div class="value">
                {{ if .YourFuckingTorExit }}
                    <span class="badge tor">Yes</span>
                {{ else }}
                    <span class="badge">No</span>
                {{ end }}
            </div>
        </div>
        <div class="footer">
            Data provided by wtfismyip.com
        </div>
    </div>
</body>
</html>
